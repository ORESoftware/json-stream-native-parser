'use strict';

// NOTE: These are exported symbols used for attaching metadata to parsed JSON values.
// They are re-exported from `src/main.ts` for backwards-compatibility.

export const RawStringSymbol = Symbol('raw.json.str');
export const RawJSONBytesSymbol = Symbol('raw.json.bytes');
export const JSONBytesSymbol = Symbol('json.bytes');


